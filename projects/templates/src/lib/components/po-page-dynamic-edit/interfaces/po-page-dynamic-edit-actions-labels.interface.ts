/**
 * @usedBy PoPageDynamicEditComponent
 *
 * @description
 *
 * Interface para os rótulos customizados das ações do componente po-page-dynamic-edit.
 */
export interface PoPageDynamicEditActionsLabels {
  /**
   * @description
   *
   * Rótulo customizado para o botão `cancel`.
   */
  cancel?: string;

  /**
   * @description
   *
   * Rótulo customizado para o botão `save`.
   */
  save?: string;

  /**
   * @description
   *
   * Rótulo customizado para o botão `saveNew`.
   */
  saveNew?: string;
}
