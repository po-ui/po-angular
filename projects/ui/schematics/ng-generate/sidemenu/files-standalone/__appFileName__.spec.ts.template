import { TestBed } from '@angular/core/testing';

import { PoMenuModule, PoPageModule, PoToolbarModule } from '@po-ui/ng-components';

import { <%= appClassName %> } from './<%= appFileName %>';

describe('<%= appClassName %>', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [PoToolbarModule, PoMenuModule, PoPageModule, <%= appClassName %>],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(<%= appClassName %>);
    fixture.detectChanges();
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });
});
