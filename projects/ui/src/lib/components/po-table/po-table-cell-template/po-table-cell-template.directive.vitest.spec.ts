import { PoTableCellTemplateDirective } from './po-table-cell-template.directive';

describe('PoTableCellTemplateDirective', () => {
  const component = new PoTableCellTemplateDirective(null);

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
