/**
 * @usedBy PoNavbarComponent
 *
 * @description
 *
 * Interface para definição das literais usadas no `po-navbar`.
 */
export interface PoNavbarLiterals {
  /** Texto exibido no item de menu no qual os itens do navbar são agrupados quando está no modo responsivo. */
  navbarLinks?: string;
}
