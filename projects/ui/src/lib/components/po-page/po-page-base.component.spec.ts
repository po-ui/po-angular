import { PoPageBaseComponent } from './po-page-base.component';

describe('PoPageBaseComponent', () => {
  const component = new PoPageBaseComponent();

  it('should be created', () => {
    expect(component instanceof PoPageBaseComponent).toBeTruthy();
  });
});
