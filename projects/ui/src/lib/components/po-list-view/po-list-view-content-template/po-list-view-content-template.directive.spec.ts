import { PoListViewContentTemplateDirective } from './po-list-view-content-template.directive';

describe('PoListViewContentTemplateDirective:', () => {
  const directive = new PoListViewContentTemplateDirective(null);

  it('should be created', () => {
    expect(directive).toBeTruthy();
  });
});
