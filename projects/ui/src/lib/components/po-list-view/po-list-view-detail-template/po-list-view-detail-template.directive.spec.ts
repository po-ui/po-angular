import { PoListViewDetailTemplateDirective } from './po-list-view-detail-template.directive';

describe('PoListViewDetailTemplateDirective:', () => {
  const directive = new PoListViewDetailTemplateDirective(null);

  it('should be created', () => {
    expect(directive).toBeTruthy();
  });
});
