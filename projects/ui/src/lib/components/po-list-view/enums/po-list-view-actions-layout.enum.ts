/**
 * @deprecated 22.x.x
 *
 * @usedBy PoListViewComponent
 *
 * @description
 *
 * Enum descontinuado. O `po-list-view` agora utiliza exclusivamente o layout padrão (po-widget por item).
 * Mantido apenas por compatibilidade de importação; o valor não tem mais efeito.
 */
export enum PoListViewActionsLayout {
  Default = 'default',
  Animalia = 'animalia'
}
