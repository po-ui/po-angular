import { PoSelectOptionTemplateDirective } from './po-select-option-template.directive';

describe('PoSelectOptionTemplateDirective', () => {
  const component = new PoSelectOptionTemplateDirective(null);

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
