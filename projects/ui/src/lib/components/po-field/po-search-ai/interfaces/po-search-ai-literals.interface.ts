/**
 * @usedBy PoSearchAiComponent
 *
 * @description
 *
 * Interface para definição das literais usadas no `po-search-ai`.
 */
export interface PoSearchAiLiterals {
  /** Texto de acessibilidade do botão de limpar o campo. */
  clean?: string;
  /** Mensagem exibida quando a busca com IA falha. */
  errorMessage?: string;
}
