import { PoMultiselectOptionTemplateDirective } from './po-multiselect-option-template.directive';

describe('PoMultiselectOptionTemplateDirective:', () => {
  const component = new PoMultiselectOptionTemplateDirective(null);

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
