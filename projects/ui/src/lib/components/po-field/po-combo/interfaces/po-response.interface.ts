/**
 * @docsPrivate
 *
 * @description
 *
 * Interface utilizada no PoComboFilterService para o retorno no padrão API TOTVS.
 * Deverá ser alterada de local após a criação do Po Resource.
 */
export interface PoResponse {
  items: Array<any>;
}
