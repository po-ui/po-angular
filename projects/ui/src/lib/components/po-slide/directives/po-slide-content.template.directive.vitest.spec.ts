import { PoSlideContentTemplateDirective } from './po-slide-content-template.directive';

describe('PoSlideContentTemplateDirective:', () => {
  const directive = new PoSlideContentTemplateDirective(null);

  it('should be created', () => {
    expect(directive).toBeTruthy();
  });
});
