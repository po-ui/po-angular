/**
 * @usedBy PoProgressComponent
 *
 * @description
 *
 * Enum para configurar a expessura (`p-size`) da barra de progresso do componente.
 */
export enum PoProgressSize {
  /** Tamanho médio, com 4px. */
  medium = 'medium',
  /** Tamanho padrão, com 8px. */
  large = 'large'
}
