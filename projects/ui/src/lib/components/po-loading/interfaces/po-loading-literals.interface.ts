/**
 * @docsPrivate
 *
 * @usedBy PoLoading
 *
 * @description
 *
 * Interface para as literais do `PoLoading`.
 */
export interface PoLoadingLiterals {
  /** Texto exibido para o usuário indicando o carregamento. */
  loading?: string;
}
