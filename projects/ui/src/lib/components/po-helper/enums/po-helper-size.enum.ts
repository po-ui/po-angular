/**
 * Tamanhos da propriedade `p-size` do PoHelper.
 * A medida `small` está disponível apenas para acessibilidade AA.
 */
export enum PoHelperSize {
  /** Altura/escala reduzida. */
  Small = 'small',
  /** Altura/escala padrão. */
  Medium = 'medium'
}
